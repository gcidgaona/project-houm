import React from 'react'

export const StatisticTitle = ({title}) => {
  return (
    <span style={{display: 'flex', justifyContent: 'center'}} className="color-sub-text"><p>{title}</p></span>
  )
}
